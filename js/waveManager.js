// js/waveManager.js
// ⭐ 保留此檔案以備兼容，主要敵人生成邏輯已移至 main.js 的 updateCombatEnemySpawn()

// 此函數現在只作為備用，由 main.js 的 updateCombatEnemySpawn 取代
function handleWavePhase(dt, currentBaseSpeed) {
    // 不再使用，保留以防其他地方引用
    return true;
}